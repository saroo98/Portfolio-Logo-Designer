# Baseline Failure Snapshot (Phase 2)

Run on unmodified handoff `index.html` after test-infra fixes (transition-aware reads, debounce waits). 75 passed, 11 skipped, **16 failed** — each maps to a planned Phase 3 fix.

| # | Test | Fix |
|---|------|-----|
| 1 | `chromium` 41 skip-link-exists | F9 |
| 2 | `chromium` 42 lightbox-focus-trap | F11 |
| 3 | `chromium` 46 theme-aria-pressed | F13 |
| 4 | `chromium` axe-bonus (aria-hidden-focus on lightbox) | F10 |
| 5 | `chromium` 47 favicon-link | F4 + F3 |
| 6 | `chromium` 48 theme-color-meta | F3 |
| 7 | `chromium` 49 first-work-priority | F16 |
| 8 | `mobile-chrome` 26 aria-expanded-and-scroll-lock | F12 |
| 9 | `mobile-chrome` 41 skip-link-exists | F9 |
| 10 | `mobile-chrome` 42 lightbox-focus-trap | F11 |
| 11 | `mobile-chrome` 45 body-scroll-lock-mobile-menu | F12 |
| 12 | `mobile-chrome` 46 theme-aria-pressed | F13 |
| 13 | `mobile-chrome` axe-bonus (aria-hidden-focus on lightbox) | F10 |
| 14 | `mobile-chrome` 47 favicon-link | F4 + F3 |
| 15 | `mobile-chrome` 48 theme-color-meta | F3 |
| 16 | `mobile-chrome` 49 first-work-priority | F16 |

Test-infra adjustments made before this snapshot (not feature changes — measurement fixes):

- **test 20 (bg-var-changes)**: read CSS custom property `--bg` directly instead of `getComputedStyle(body).backgroundColor`, which lags during the 320 ms background transition.
- **test 22 (open-drawer)**: poll opacity until the 280 ms drawer fade settles, instead of reading mid-transition.
- **test 36 (arrow-keys-navigate)**: insert `waitForTimeout(500)` between arrow presses to clear the intentional 440 ms `isSwapping` debounce inside the lightbox swap.

Fixes that do **not** appear as failing tests (improve robustness, not behavior):

- F1 (theme-init in `<head>`, prevents FOUC)
- F2 (`<noscript>` fallback for images)
- F3 (canonical, OG, Twitter, JSON-LD — only theme-color/favicon parts caught by tests 47, 48)
- F5 (`type="button"` on all 5 buttons)
- F6 (`aria-hidden="true"` on decorative SVGs)
- F7 (capitalize nav `aria-label`)
- F8 (lightbox dialog `aria-label`)
- F14 (lightbox `addEventListener('load', …, { once:true })` instead of `onload =`)
- F15 (drop always-on `will-change: transform`)
